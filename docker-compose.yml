services:
  livros-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: livros-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: default
